import 'package:flutter/material.dart';

/// Placeholder — puna implementacija dolazi u Fazi 3
/// (AppHeroSection, CountryCityPicker, PlanOptionsView, InterestChipsGrid).
class MainScreen extends StatelessWidget {
  const MainScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Plan — dolazi u Fazi 3')),
    );
  }
}
